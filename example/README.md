example
=======

This is the home of an example project for the GlaMBIE-2 code playground.

Run the example script from the top-level directory as follows:

```bash
python -m example.script
# Hello, world!
```
